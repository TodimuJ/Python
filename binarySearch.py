def binarySearch(array, number):
    bIndex = 0
    eIndex = len(array) -1 

    while bIndex <= eIndex:
        midpoint = bIndex + (eIndex - bIndex) // 2
        midpoint_value = array[midpoint]

        if 
